# ROS message used for mission execution status reporting
bool mission_active # True if any primitive is active
string active_primitive # Active primitive name
bool mission_execution_ready # True if mission execution initialized
bool manual_enabled # True if manual is enabled

